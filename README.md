# MovieSearcher

Je hoeft nu alleen dit te doen:

1. Start backend:
```bash
node server.js
```
2. Open je site op `http://127.0.0.1:5500/` (of `http://localhost:3000`)

API key staat al goed in `.env`.

Als zoeken nog `Failed to fetch` geeft, draait `server.js` niet op poort 3000.
